<footer class="footer">
    <hr />
    <div class="bg-white w-75 mx-auto">
        <span class="text-black float-right">
            <%= link_to "Home", root_path, class: "text-muted mr-2" %>
            <%= link_to "About", about_path, class: "text-muted mr-2" %>
            <% if logged_in? %>
                <%= link_to "Setting", edit_user_path(current_user), class: "text-muted mr-2" %>
            <% end %>
        </span>
        <p class="text-black">Elearnig application © Ryoma Oshimoto</p>
    </div>
</footer>